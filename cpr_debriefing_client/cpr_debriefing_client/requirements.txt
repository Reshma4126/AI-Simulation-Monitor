# =============================================================================
# PyTorch (CUDA) — install MANUALLY via the wheel index below.
# Do NOT install with plain `pip install torch` — that gives CPU-only build.
#
#   pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# For CPU-only (slower):
#   pip install torch torchaudio
# =============================================================================

# Audio transcription
faster-whisper>=1.0.0          # venv: 1.2.1 — requires torch for CUDA inference

# Speaker diarization
pyannote.audio>=3.1.1          # requires HF_TOKEN; uses 'token=' kwarg (not use_auth_token)

# NLP
spacy==3.7.2

# FSM
transitions==0.9.0

# Data handling
pandas==2.1.0
numpy>=1.26.0
scipy>=1.11.0

# Audio processing
pydub>=0.25.1                  # used in whisper_pipeline for preprocessing
librosa>=0.10.1
soundfile>=0.12.1

# Report generation
reportlab==4.0.7

# Web server
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0

# API integrations
anthropic==0.25.0
openai>=1.30.0           # used by ollama_api.py (OpenAI-compatible endpoint)

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0

# Testing
pytest==7.4.0
